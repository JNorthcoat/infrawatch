{
  "name": "sydney-infrawatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "format": "prettier --write src",
    "update-prices": "node scripts/update-prices.mjs",
    "update-prices:force": "node scripts/update-prices.mjs --force",
    "update-prices:verify": "node scripts/update-prices.mjs --verify"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "prettier": "^3.5.3",
    "unzipper": "^0.12.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  }
}
